---

# `requirements.txt`

For this script, **no external libraries** are used — only Python’s built-in modules. Still, include a `requirements.txt` for clarity:

```txt
# No external packages required